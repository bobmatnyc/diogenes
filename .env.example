# Clerk Authentication Configuration (Required)
# Get these from your Clerk Dashboard: https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key_here
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key_here

# Clerk Redirect URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat

# OpenRouter API Configuration (Required for chat functionality)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Application URL (for OpenRouter referrer)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google Analytics Configuration (Optional)
# Get your measurement ID from Google Analytics
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Search API Configuration (Optional)
# For production, use one of these search APIs:
# Option 1: Tavily (Recommended for LLM use)
TAVILY_API_KEY=your_tavily_api_key_here

# Option 2: Serper API
# SERPER_API_KEY=your_serper_api_key_here

# Delegation Configuration
# Enable mock mode for testing without API calls
ENABLE_MOCK_SEARCH=false

# Development/Debug Settings
# WARNING: Do NOT set NODE_ENV in .env.local - it causes Next.js 15 build failures
# Next.js automatically manages NODE_ENV:
# - development: set automatically during 'npm run dev'
# - production: set automatically during 'npm run build'
# NODE_ENV=development  # ❌ DO NOT UNCOMMENT THIS LINE

# Optional: Perplexity-specific settings if using direct Perplexity API
# PERPLEXITY_API_KEY=your_perplexity_api_key_here

# ============================================
# Kuzu Memory Configuration
# ============================================
# Maximum number of memories to store per user (default: 1000)
MEMORY_MAX_PER_USER=1000

# Number of days to retain memories before auto-deletion (default: 30)
MEMORY_TTL_DAYS=30

# Enable automatic memory extraction from conversations (default: true)
MEMORY_AUTO_EXTRACT=true

# Vercel Blob Storage (Optional - for production)
# Required only if deploying to Vercel and using Blob storage for memories
# BLOB_READ_WRITE_TOKEN=vercel_blob_your_token_here

# ============================================
# Clerk Setup Instructions:
# ============================================
# 1. Go to https://dashboard.clerk.com/
# 2. Create a new application or select existing
# 3. Go to Configure → Authentication → Social OAuth
# 4. Enable Google OAuth provider
# 5. Add your Google OAuth credentials:
#    - Client ID: From Google Cloud Console
#    - Client Secret: From Google Cloud Console
# 6. Copy your Clerk keys from the Dashboard:
#    - API Keys → Copy Publishable Key
#    - API Keys → Copy Secret Key
# 7. For production deployment:
#    - Update NEXT_PUBLIC_APP_URL to your production URL
#    - Add production domain to Clerk Dashboard
# ============================================