{
  "name": "diogenes",
  "version": "0.2.2",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "version:major": "npm version major && git push && git push --tags",
    "version:minor": "npm version minor && git push && git push --tags",
    "version:patch": "npm version patch && git push && git push --tags",
    "version:prerelease": "npm version prerelease --preid=rc && git push && git push --tags",
    "version:current": "echo Current version: v$(node -p \"require('./package.json').version\")",
    "changelog": "echo 'Generating changelog...' && git log --oneline --decorate --color",
    "release": "npm run build && npm run version:patch",
    "release:minor": "npm run build && npm run version:minor",
    "release:major": "npm run build && npm run version:major"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.27",
    "@ai-sdk/react": "^2.0.39",
    "ai": "^5.0.39",
    "axios": "^1.11.0",
    "js-tiktoken": "^1.0.21",
    "jsdom": "^26.1.0",
    "next": "^15.5.2",
    "node-fetch": "^3.3.2",
    "openai": "^4.79.0",
    "playwright": "^1.55.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
