# ============================================
# Development Configuration for Clerk Auth Bypass
# ============================================
# Copy this file to .env.local for local development

# Clerk Authentication Configuration (Optional in Development)
# In development mode, authentication is automatically bypassed on localhost
# You can leave these empty or use test keys for development
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Force Authentication in Development (Optional)
# Set to "true" to require Clerk authentication even in development mode
# Leave unset or set to "false" for automatic auth bypass on localhost
# NEXT_PUBLIC_FORCE_AUTH_IN_DEV=false

# Development User Configuration
# When auth is bypassed, a default user "Bob Matsuoka" is used
# User ID: dev_user_bob_matsuoka
# Email: bob@localhost.dev

# Clerk Redirect URLs (Only used when auth is enabled)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/chat
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/chat

# ============================================
# Required: OpenRouter API Configuration
# ============================================
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ============================================
# Optional: Memory API Configuration
# ============================================
# Memory API key for development (if using memory features)
NEXT_PUBLIC_MEMORY_API_KEY=internal_diogenes_key

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Optional: Analytics (Production Only)
# ============================================
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# ============================================
# Optional: Search Configuration
# ============================================
# TAVILY_API_KEY=your_tavily_api_key_here
# ENABLE_MOCK_SEARCH=false

# ============================================
# Development Notes:
# ============================================
# 1. In development mode on localhost, Clerk authentication is automatically bypassed
# 2. The dev user "Bob Matsuoka" (ID: dev_user_bob_matsuoka) is automatically used
# 3. Memory system will work with the dev user ID consistently
# 4. To test with real Clerk authentication in dev, set NEXT_PUBLIC_FORCE_AUTH_IN_DEV=true
# 5. Production deployments will always use proper Clerk authentication
# ============================================